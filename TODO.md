# Nemesh — TODO

## In Progress

## Backlog

- [ ] In sx style objects, order properties so `display` comes first (if present), then `position` second (if present). Apply this consistently across all existing style files.

## Done
